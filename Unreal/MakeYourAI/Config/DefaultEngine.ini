; No project map is configured until a real .umap is saved by Unreal Editor.
; Nanite, Lumen, shadow and World Partition settings are deliberately not
; represented as verified by this file. Configure and measure them in the gate.
